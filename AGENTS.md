# AGENTS.md

本文件用于约束 AI 编码代理在当前仓库内的开发、分析、实验和文档行为。所有代理必须优先遵循本文件；若与用户的最新明确指令冲突，以用户最新指令为准。

## 1. 基本原则

- 全程使用简体中文回复、记录日志、编写文档和提交说明；代码标识符沿用项目既有命名风格。
- 事实优先，不得编造实验结果、控制台日志、评测指标或文件内容。
- 简洁清晰优于详尽堆砌；避免生成不必要的报告、指南和总结文档。
- 标准化和生态复用优先：优先复用官方 SDK、成熟库、项目既有模块和通用工具，避免重复造轮子。
- 问题驱动优先于流程驱动：流程服务于解决问题，不机械执行无意义步骤。

## 2. 数据真实性与实验记录

- 任何数字、指标、结论必须来自真实磁盘文件、真实配置、真实日志或真实控制台输出。
- 禁止“模拟日志”“合理猜测”“预期数值”冒充真实结果。
- 分析实验前，必须优先读取真实输出文件，例如 `runs/<run_id>/logs/metrics.csv`、JSON、TensorBoard 日志或评测结果文件。
- 如果命令失败、超时或文件缺失，必须原样说明错误并停止当前推断，不得补造结果。
- 用户明确表示不需要运行实验时，不得主动启动训练、评测或长流程实验。
- 需要贴结果时，只贴真实文件或真实控制台输出的原始关键片段，并说明来源路径。

## 3. 代码修改权限

- 修改代码前必须先询问用户并获得明确同意。
- 如果用户明确说“先不要改”“先讲清楚”“先解释原理”，必须先完整说明问题原理、影响范围和修改方案，等待确认后再改。
- 获得明确同意后，直接保存文件，不需要对每个小改动重复确认。
- 不得在缺乏充分上下文的情况下编码。
- 不得偏离项目既有结构、命名、格式化规则和测试约定。

## 4. 上下文收集要求

编码前必须完成足够的上下文分析：

- 快速扫描项目结构，识别关键目录、入口文件、配置文件、测试框架和日志位置。
- 至少分析 3 个相似实现、组件或调用模式，确认可复用接口和约束。
- 明确依赖与集成点，包括输入输出协议、配置项、环境要求和边界条件。
- 优先查阅官方文档、项目内文档、成熟实现或已有代码示例，避免凭记忆猜测。
- 进入修改前，应能回答：接口契约是否清晰、技术选型理由是否理解、主要风险是否识别、验证方法是否明确。

## 5. 实现规范

- 每个函数或类只承担单一责任。
- 禁止过早抽象；同类逻辑重复三次以上再考虑通用化。
- 禁止使用难以维护的“聪明技巧”，优先可读性和可审计性。
- 注释只解释意图、约束和使用方式，不重复代码逻辑。
- 禁止在代码注释中写“本次修改说明”，变更信息应写入日志或版本记录。
- 所有代码文件使用 UTF-8 无 BOM 编码读写。

## 6. 测试与验证

- 每次实现应提供可自动运行的单元测试、冒烟测试或功能测试。
- 测试应覆盖正常路径、边界条件和典型错误场景。
- 无法补充测试时，必须在日志中记录风险、原因、补测计划和建议截止时间。
- 验证必须基于真实命令输出、真实测试结果或真实文件内容。
- 涉及性能或准确率时，必须说明评测数据、日志路径、可视化目录和评测输出保存位置。

## 7. Shell 与命令执行

- 只运行用户明确授权的命令；涉及实验、训练、评测的命令尤其需要确认。
- 给用户提供 CMD 命令时，若命令不长，优先写成单行，便于直接复制粘贴。
- 若终端出现确认提示，并且用户已指定默认选择，则按用户指定选项执行。
- 命令失败时，立即报告错误片段和失败位置，不继续基于失败结果推断。

## 8. 日志与文档管理

### 8.1 代码修改日志

所有代码修改必须追加记录到项目根目录下的 `代码修改日志/` 文件夹。

- 日志文件名必须包含日期，例如：`代码修改日志_YYYY-MM-DD.md`。
- 每次记录至少包含：修改时间、修改文件、修改目的、修改内容摘要、验证情况。
- 每一项代码改动都必须记录“修改前”和“修改后”的关键对照内容。

### 8.2 CHANGELOG

除代码修改日志外，工作记录只写入当天 CHANGELOG，例如：`CHANGELOG_YYYY-MM-DD.md`。

CHANGELOG 应记录：

- 问题现象
- 诊断过程
- 修复方案
- 验证结果

不得额外生成大量分析报告、快速指南、总结文档或重复说明材料。

### 8.3 公式记录要求

日志中凡出现公式，必须同时给出：

- 变量定义表：每个变量的含义、张量形状或取值范围、单位或语义。
- 公式用途说明：该公式用于哪个阶段、解决什么问题、对结果的预期影响。

## 9. 质量审查

完成实现后必须进行自查，至少覆盖：

- 需求字段是否完整：目标、范围、交付物、审查要点。
- 是否覆盖用户原始意图，无遗漏或歧义。
- 交付物映射是否明确：代码、文档、测试、验证结果。
- 依赖、风险和回滚方式是否清楚。
- 审查结论是否留痕并包含时间戳。

建议判定规则：

- 综合评分 ≥ 90 且建议“通过”：确认通过。
- 综合评分 < 80 且建议“退回”：确认退回并继续修复。
- 80–89 或建议“需讨论”：重点复核风险项后再决策。

## 10. 联邦学习实验特殊要求

涉及联邦学习实验时，必须遵循：

- 所有训练客户端必须使用 Non-IID 数据分布，禁止默认使用 IID。
- 必须体现数据异构性，避免不符合真实联邦学习场景的设置。
- 出现“1 轮就收敛”等异常现象时，必须优先排查数据泄漏、评测逻辑、训练轮次、学习率、划分方式和日志记录问题。
- 准确率未达到目标阈值时，不得编造结果；应基于真实日志继续分析原因，并在获得用户授权后再修改或运行实验。

## 11. 沟通风格

- 优先直接对话说明关键进展，不频繁生成任务清单。
- 只有复杂多步骤任务确实需要跟踪时，才使用清单。
- 不反复询问已知信息；但涉及代码修改、实验运行或破坏性操作时必须确认。
- 报告失败时直接说明失败原因、证据和下一步建议，不用“预计”“应该”等词伪装确定性。
